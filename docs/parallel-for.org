#+startup: beamer
#+latex_header: \usepackage{tikz}
#+latex_header: \usetikzlibrary{graphs,quotes,graphdrawing}
#+latex_compiler: lualatex
#+latex_header: \usegdlibrary{trees}

* Mark
#+begin_export latex
\begin{tikzpicture}
  \graph [grow down, tree layout, nodes={rounded corners, rectangle, draw}] {
    "\texttt{{band\_node}}" -- {
      "",
      "",
      "\texttt{band\_node}" -- { "", "" }
    }
  };
  % TODO: draw arc around the marked subtree
  \scoped [xshift=5cm]
  \graph [grow down, tree layout, nodes={rounded corners, rectangle, draw}] {
    "\texttt{{band\_node}}" -- {
      "",
      "",
      "\texttt{mark:{"parallel"}}"[red] -- {
        "\texttt{band\_node}" [>red] -- { "", "" }
      }
    }
  };
  % TODO: color mark node red
\end{tikzpicture}
#+end_export
